{
  "name": "run-dispatcher",
  "version": "0.0.0",
  "private": true,
  "description": "t2000-run dispatch Worker — routes paid deliveries to hosted seller handlers (Workers for Platforms)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
