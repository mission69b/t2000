---
description: Confidential AI (GPU-TEE) verifiable inference — anchor-every on Sui, receipts in Redis (NOT Walrus), client-trustless verify, honest trust boundary. Full architecture in .claude/skills/t2000-confidential-verify/.
globs: packages/sdk/src/verify.ts,packages/cli/src/commands/verify.ts,apps/verify/**,contracts/confidential_anchor/**
alwaysApply: false
---

# Confidential AI → `.claude/skills/t2000-confidential-verify/SKILL.md`

**The invariants that get overclaimed if you skip this:**

- Durable receipts live in **Redis, NOT Walrus** (the pin-and-own plan was reversed).
- Confidential = **pure in-TEE completion** — no tools, web search, artifacts, or
  memory, or the 🔒 badge is a lie.
- **Anchor-every**, not on-demand; the `POST /anchor/{id}` endpoint is backfill only.
- **Honest framing is mandatory** — TDX + signed receipt + Sui anchor are trustless;
  the gateway's plaintext leg is ZDR, not E2E. Never label routed/frontier responses
  "confidential", and never present a server-side checkmark as client-side proof.

**Read the full chain, invariants, and file map before touching the confidential
path, receipts, anchoring, or any verify surface:**
`.claude/skills/t2000-confidential-verify/SKILL.md`.

*(Content moved there 2026-07-24 — do not re-inline it here; this file is a pointer
so Cursor and Claude Code cannot drift.)*
