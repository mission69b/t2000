# SPEC — AI SDK Hardening

> **Status:** SHIPPING · drafted 2026-05-24 · revised 2026-05-25 (Phases 1 + 2 + 3 + 4 + 5 + 6 ALL closed — P4.1 shipped as engine v3.0.0 on 2026-05-25; v3.1.0 follow-up shipped same day for dead `todoUpdateCount` column + `LLMProvider` cleanup + CLI/MCP eslint configs; **Phase 7 — P7.2 `inputCoinFromStep` + P7.3 `cetusRoute` fast-path threading SHIPPED 2026-05-25 (S.314); only P7.5 streaming bundle assembly progress UX remains**) · author: agent (Opus 4.7)
> **Local-only?** No — this SPEC is tracked. `audric-build-tracker.md` references stay founder-local.
> **Promoted from:** `.cursor/plans/ai_sdk_hardening_bc37c5e8.plan.md` (the working plan that drove this work — original is the source of truth for full per-item rationale + AI SDK doc citations).
> **Why tracked:** the plan has 7 phases / 32 items across ~3 weeks of work. Multiple agents and sessions will work on it. Without a tracked SPEC, the next agent risks re-shipping completed phases (the exact failure mode that triggered this promotion).

---

## 1. Goal

Close the gap between Audric's AI SDK v6 integration and Vercel's documented best practices. Evidence: subagent survey of `packages/engine/src/v2/define-tool.ts`, `audric/apps/web-v2/components/audric/tool-result-router.tsx`, and the host stream handler at `audric/apps/web-v2/app/api/chat/route.ts` cross-referenced against the AI SDK docs (foundations/tools, chatbot-tool-usage, prompt-engineering, loop-control, building-agents, workflows, tool-approval, artifacts).

## 2. Sequencing rationale (per the plan)

1. **Phase 1** — Tier 1 user-visible bugs · ships first
2. **Phase 2** — Tier 4 hygiene (low risk, sets up Phase 4 safely)
3. **Phase 5** — Tier 2 chat UX gaps (visible-to-user impact)
4. **Phase 6** — Tier 2 error handling (depends on Phase 5 Edit for user-side recovery)
5. **Phase 3** — Tier 2 best-practice alignment (host-level wins, no engine bump)
6. **Phase 7** — Bundle hardening (gated on Phase 3 stability)
7. **Phase 4** — Tier 3 architectural (defineTool migration touches every tool)

## 3. Phase status

### Phase 1 — Tier 1 bugs ✅ SHIPPED 2026-05-24 (S.284)

- ✅ **P1.1** Fix JSON-flash after confirm — `tool-result-router.tsx` gains `approval-responded` branch
- ✅ **P1.2** Forward `tool-input-start/delta/end` chunks through `translateChunk`
- ✅ **P7.1** Defensive `MAX_BUNDLE_OPS` enforcement in `BundleBuffer.flush()` (Option A: synthesize `tool-output-error` for overrun legs)

Audric commit: `6f9d940`. Live smoke green at 08:33 AEST. No engine bump.

### Phase 2 — Tier 4 hygiene ✅ SHIPPED 2026-05-24 (S.285 + S.286-S.289)

- ✅ **P2.1** `.optional()` → `.nullable()` audit (13 fields across 6 tool schemas) — engine v2.19.3
- ✅ **P2.2** Stream resume host wiring — extracted into companion SPEC `SPEC_AUDRIC_STREAM_RESUME.md` (Phases 1, 1.5, 2, 3 all shipped). Single largest slice of Phase 2.
- ✅ **P2.3** `defaultSettingsMiddleware` wraps chat model (temperature 0.3, maxOutputTokens 8192)
- ✅ **P2.4** `TelemetryIntegration` native-lifecycle audit (custom kept; rationale documented inline)
- ✅ **P2.5** Redis-backed IP rate limit on `/api/chat` (30 msgs / 60s / IP, degrade-open posture)

Audric commits: `45a457f` (P2.3+P2.4+P2.5), then `0e3812f`/`6dca31d`/`afa83cb`/`2a8922e`/`2239d30` for the SPEC_AUDRIC_STREAM_RESUME phases. Engine bumped 2.19.2 → 2.19.3.

### Phase 5 — Chat UX gaps ✅ SHIPPED 2026-05-24 (S.290 + S.291 + S.292)

- ✅ **P5.5** Stop button + abort plumbing — shipped via `SPEC_AUDRIC_STREAM_RESUME` Phase 2 (S.288 client wiring) + Phase 3 (S.289 cross-instance abort). Cross-references the same `useChat` invocation.
- ✅ **P5.2** Copy buttons on user + assistant rows — `getMessageText` helper + `MessageAction` wiring (S.290, audric `313f6ed`)
- ✅ **P5.3** Vote hydration from `GET /api/vote` on chat mount — batched fetch + `initialVote` prop on `MessageVoteThumbs` (S.290, audric `313f6ed`)
- ✅ **P5.4** Regenerate action on last assistant row — `useChat.regenerate()` no-args (S.290, audric `313f6ed`). Per-message regenerate (with `messageId`) deferred to P5.1 / Edit territory.
- ✅ **P5.1** Edit user message + re-send + `truncateMessagesAfter` server helper + 5 chat-persistence integration tests (11 cases) folded in (S.291, audric `9029eb1`). Includes a `bundle-status.ts` extraction so `isBundleSpent` is unit-testable. Self-audit caught + fixed a legacy-schema edge case (penultimate `{role, content}` without `id` would have triggered a full-chat wipe; now skipped with a warn log).
- ✅ **P5.6** Surface live HF/APY metadata through `buildAudricToolMetadata` + PermissionCard (S.292). New `lib/audric/live-data.ts` host-side enrichment module replicates the engine's `enrichPendingActionWithLiveData` subset (the engine's helper is unreachable from web-v2's `Experimental_Agent` path). `currentHF` / `projectedHF` / `borrowApyBps` thread through `tool-input-available` → `parseAudricMetadata` → `PermissionCard` → `renderPreviewBody`, lighting up the existing HFRow / APYRow rich previews. 19 unit tests for `projectHF` + `computeMetadataEnrichment` cover the 8-tool decision matrix + graceful degradation. **Quote-refresh UI + per-reward breakdown + cetusRoute deferred to backlog per Round 3 trim** — first-time borrowers (no existing position) still see the existing "Variable rate" disclaimer rather than a live APY.

### Phase 6 — Error handling alignment ✅ SHIPPED 2026-05-24 (S.298)

- ✅ **P6.1** `onError` callback on `createUIMessageStream` (the correct seam — plan said `createUIMessageStreamResponse` but AI SDK types put `onError` on the stream constructor, NOT the response wrapper; corrected during implementation)
- ✅ **P6.2** Client `useChat({ onError })` minimal observability hook — banner-only UX (finance app · recovery via P5.1 Edit + P5.4 Regenerate is the gentle path · revisit if telemetry shows banner blindness)
- ✅ **P6.3** Replace string-heuristic error classification with typed AI SDK error classes — `classifyStreamError()` does `isInstance` first (RetryError / APICallError with status-code switch / NoSuchToolError / InvalidToolInputError / InvalidToolApprovalError / ToolCallNotFoundForApprovalError), falls back to the heuristic sanitizer for raw strings. Back-compat `sanitizeStreamErrorMessage(raw)` preserved for the engine-chunk error path. 29 unit tests; 64/64 lib/audric tests pass.

Audric commit: `ab4f789`. No engine bump (host-only). No new dependencies.

### Phase 3 — Best-practice alignment ✅ CLOSED

- ✅ **P3.1** `prepareStep.activeTools` (intent classifier v1 cuts 26 → 5-12 tools per turn) (S.302 — 2026-05-24). Three new web-v2 modules: `lib/audric/intent-classifier.ts` (pure regex classifier over 9 finance intents — `save`, `borrow`, `send`, `swap`, `rewards`, `history`, `portfolio`, `paymentLinks`, `rates` — with `general` fallback; each intent maps to a 4-7 tool subset that includes post-write refresh reads), `lib/audric/active-tools-prepare-step.ts` (per-request closure that classifies at step 0 + caches for steps 1+ so the active subset is stable across multi-step turns; resume turns with empty user text → general fallback), `lib/audric/compose-prepare-steps.ts` (parallel composer merging memwal's `system` return + active-tools' `activeTools` return — same `Promise.all` parallelism so no serial latency penalty). Workflow phrasings (`rebalance`, `diversify`, `auto-save`) trigger multi-intent unions so compound writes still have the right tools. Gateway path opts `perplexity_search` in via `alwaysInclude` since web-search is intent-orthogonal. 55 new unit tests (38 classifier + 12 prepare-step + 7 composer). 167/167 web-v2 tests pass. Heuristic v1 chosen over LLM-based classifier to avoid 200-400ms per-turn latency tax + extra LLM cost; misclassifications fall through to `general` (still gives the LLM the most common read tools). Expected reduction: ~3K-5K prompt tokens per step on typical single-intent turns, improved Anthropic prompt-cache hit rates on similar-intent turns.
  - 🩹 **P3.1 hotfix (S.309 — 2026-05-24)** — Production smoke caught two structural gaps in the original P3.1 ship: (1) `TOOLS_BY_INTENT.general` was reads-only despite the docstring's "Conservative-by-construction" claim — misclassified turns stripped writes from activeTools and the model hallucinated tool absence ("I don't have a save_deposit tool"); (2) the classifier was stateless per-turn, so a 2-word conversational follow-up ("yea lets go") in the middle of an established save flow re-classified from scratch with zero context. Combined with a user typo (`yeild` defeating `/yield/i`), the smoke produced a complete tool-availability failure. Fixes: (a) `extractPreviousUserMessage` + carryover logic in `active-tools-prepare-step.ts` — when current turn is `low` confidence, inherit the previous user message's intent if it was high/medium (new `outcome=carried-over` log value); (b) widened `TOOLS_BY_INTENT.general` to include 6 common writes (`save_deposit`, `withdraw`, `send_transfer`, `borrow`, `repay_debt`, `swap_execute`) — degrade-open per the docstring's promise. +9 unit tests; 179/179 web-v2 tests pass. Same commit also removed dead `temperature: 0.3` config from `defaultSettingsMiddleware` (S.211 always-on thinking made it provider-rejected for ~2 weeks, emitting a noise warning every turn — cleanup, zero behavior change).
- ✅ **P3.2** Enable `experimental_repairToolCall` on `Experimental_Agent` (S.300 — 2026-05-24). `lib/audric/tool-call-repair.ts` factors the callback: `NoSuchToolError` returns null (model re-plans), `InvalidToolInputError` does a single structured-output `generateText` call with the tool's JSON Schema + the validation error and returns the repaired `toolCall.input`. Graceful null-fallback whenever the secondary call itself fails. 10 unit tests. Stale `Option B (post-P3.2)` comment in `BundleBuffer.flush()` updated to note the seam is now wired (the BundleBuffer A→B swap stays as a follow-up; it needs bundle-aware context, not just schema validation).
- ✅ **P3.3** CLOSED as dormant strategic seam (S.303 — 2026-05-24). Decision doc shipped at `spec/reference/MCP_PROMPTS_INTEGRATION_DECISION.md`. The original "wire OR delete `t2000-skills/`" framing was wrong on both options: (a) wiring `t2000-skills/` into web-v2 today would duplicate guidance already in `apps/web-v2/lib/audric/system-prompt.ts`, surface CLI-flavored vocabulary (`t2000 save 80`, suiscan URLs) to web-v2 users, and re-inflate the prompt tokens P3.1 just trimmed; (b) deleting `t2000-skills/` would break the shipped `@t2000/mcp` npm package which serves Cursor / Claude Desktop / Codex CLI per the Earns Its Keep audit (2026-05-23, §3.4 — "all 14 KEEP today"); (c) deleting the engine plumbing (`McpPromptAdapter` + `skillRecipeBlock`) would foreclose the v0.7a §3 strategic option of "speak any Sui protocol's MCP" — when (not if) Cetus / DeepBook / Volo expose prompts via their MCPs, the wiring is one config entry instead of an engine release. The McpClientManager TOOLS counterpart is already live in production (`audric/apps/web-v2/lib/audric/navi-mcp.ts` against NAVI MCP) — the prompts adapter is intentionally symmetric and dormant. Activation criteria documented in the decision doc. Cost of dormancy: ~100 LoC adapter + 5 unit tests + 1 optional `EngineConfig` field. Cost of rebuild-if-deleted: significantly higher. Zero production code change in P3.3; only the dormant-comments in `system-prompt.ts:535` + `route.ts:1145` updated to point to the decision doc, and this SPEC line itself.
- ✅ **P3.4** Unify post-write logic via engine `onStepFinish` (S.301 — 2026-05-24). Engine 2.20.0 publicly exports `buildStepFinishHandler` + `StepFinishMutableState`; audric/web-v2's `Experimental_Agent` now wires `onStepFinish: buildStepFinishHandler([...READ_TOOLS, ...WRITE_TOOLS], internalContext, stepFinishMutable)` so four post-step concerns the legacy `AISDKEngine.submitMessage` path runs automatically finally reach the v2 host path: (1) `updateGuardStateAfterToolResult` (guard trackers — swap_quote pairing, balance freshness, retry counts, lastHealthFactor), (2) `extractTrustedAddressesFromResult` (lookup_user / resolve_suins → guardState.trustedAddresses), (3) sessionSpend USD accumulation + `onAutoExecuted` hook (Upstash daily-spend ledger increment via `incrementSessionSpend(sessionId, usdValue)` — closes the dead Group E TODO at `translateChunk` → tool-result case), (4) `clearPortfolioCacheFor` + `clearDefiCacheFor` (BV 60s cache invalidation after every successful write so the next balance_check / portfolio_analysis hits fresh state — ALSO load-bearing for host PWR pre-fired reads). `buildInternalContext({...})` now passes `onAutoExecuted` + `permissionConfig` + `priceCache` (the latter two as the SAME references the toolContext holds). Host PWR module stays — it pre-runs balance_check / savings_info / health_check at the START of the resume turn (different concern than guard / cache / spend bookkeeping). `pnpm typecheck` clean, `pnpm lint` clean, 112/112 web-v2 tests pass. The four concerns no longer drift between the legacy and v2 paths.

### Phase 7 — Bundle hardening (remainder) ⏳ PARTIAL (P7.2 + P7.3 SHIPPED 2026-05-25 / S.314; P7.5 remains)

- ✅ **P7.1** Cap enforcement — already shipped in Phase 1 (defensive Option A; swap to Option B after P3.2 lands for cleaner repair-tool-call recovery)
- ✅ **P7.2** Wire `inputCoinFromStep` end-to-end SHIPPED 2026-05-25 (S.314). Engine `composeBundleFromToolResults` already populated `step.inputCoinFromStep` for whitelisted (producer, consumer) pairs whose assets aligned (since SPEC 13 Phase 1 / engine 1.13.0); the marker layer in audric/web-v2 was dropping it. Threaded through `AudricBundleMarker.steps[i].inputCoinFromStep` → `AudricBundleMarkerData` mirror in `bundle-status.ts` → `parseAudricBundleMarker` defensive parse → client dispatch in `audric-chat-client.tsx` → `SponsoredTxBundleStep.inputCoinFromStep` → `bundleStepSchema.inputCoinFromStep` (Zod `z.number().int().nonnegative().optional()`) → `buildBundleSteps` `WriteStep.inputCoinFromStep` passthrough. Engine `compose-bundle.test.ts` gained 5 chain-mode invariant tests (asset-aligned whitelisted pair populates → assets misaligned doesn't → non-whitelisted pair doesn't → 3-op DAG with two chained pairs → forward-only `< i` lock). Without this, chained-asset bundles like `swap_execute(USDC → USDsui) → save_deposit(USDsui)` reverted at PREPARE because the SDK's wallet pre-fetch couldn't find USDsui that hadn't been swapped yet. No engine release needed (compose-bundle.ts behaviour unchanged, only new tests). Audric `apps/web-v2` plumbing only.
- ✅ **P7.3** cetusRoute fast-path threading SHIPPED 2026-05-25 (S.314). Engine `composeBundleFromToolResults` already accepted `swapQuoteReads: SwapQuoteReadEntry[]` and populated `step.cetusRoute` for matching `swap_execute` steps via `findMatchingCetusRoute`; the chat-route was passing `swapQuoteReads: undefined`. Added a turn-wide `swapQuoteReads: SwapQuoteReadEntry[]` accumulator in the stream handler: captures `swap_quote` `tool-call` inputs (by toolCallId) + pairs with `tool-result` `serializedRoute` outputs (+ Date.now() timestamp), passes through `BundleBuffer.flush(..., swapQuoteReads)` → `buildBundleMarker` → `composeBundleFromToolResults({swapQuoteReads})`. Marker carries serialized route through to client; `parseAudricBundleMarker` does structural shape check; `SponsoredTxBundleStep.cetusRoute` forwards as opaque `unknown` payload; `bundleStepSchema` does loose Zod structural validation (routerData/amountIn/amountOut/etc.); `buildBundleSteps` calls `deserializeCetusRoute(cetusRoute)` and spreads into `swap_execute` step's `input.precomputedRoute` so the SDK's `addSwapToTx` skips its ~150-200ms `findSwapRoute()` discovery latency per leg. Try/catch around deserialize falls back to fresh discovery on shape mismatch (identical correctness as pre-P7.3). No engine release needed (logic unchanged, only chat-route wiring). Audric `apps/web-v2` plumbing only.
- ✅ **P7.4** Bundleable flag — shipped with P4.1 via `tool-flags.ts` central registry (`getToolFlags(name)` + `isBundleableTool(name)` resolve from the registry; no per-tool `.flags` property on the tool object).
- ⏳ **P7.5** Streaming bundle assembly progress UX (`data-audric-bundle-progress` parts) — DEFERRED. Phase 7 close requires this or an explicit cleanup decision. Scope: new `data-audric-bundle-progress` UI stream parts emitted during the post-approval prepare → sponsor → sign → execute pipeline so the user sees per-step "preparing / signing / executing" state instead of one opaque latency window. Different mental model from P7.2/P7.3 (which operate at the compose layer); P7.5 runs entirely during the execute phase and needs new client state machine + new stream parts. ~1-2d when picked up. Trigger criteria: post-P7.2/P7.3 smoke surfaces user confusion during bundle execute latency (≥3-5s prepare-to-receipt windows for 3-op DAGs).

### Phase 4 — Architectural ✅ SHIPPED

- ✅ **P4.1** SHIPPED 2026-05-25 — engine v3.0.0 major. Every production tool migrated to native AI SDK `tool()` shape; `defineTool()` factory deleted from public API along with the legacy `Tool` interface, `applyToolFlags`, `toAISDKTools`, `wrapLegacyTool`, `TxMutex`, `runTools`, `EarlyToolDispatcher`, `QueryEngine`, `ToolSchema`, `pending_input` event, plus the dead `add_recipient` + `updateTodoTool` tools.
  - 🧹 **P4.1 follow-up SHIPPED 2026-05-25 — engine v3.1.0 minor.** Three threads of dead-code cleanup that fell out of the v3.0.0 audit: (f1) Audric's `TurnMetrics.todoUpdateCount` Prisma column dropped (engine no longer emits `todo_update` events post-v3.0.0; the host was writing `0` on every row by construction) — Prisma migration `20260525000000_p41_drop_todo_update_count` deployed to prod Neon via Vercel build hook. (f2) Engine `LLMProvider` legacy pathway fully retired: deleted `AISDKAnthropicProvider` + its test + the legacy `LLMProvider` / `ChatParams` / `ToolDefinition` types + `EngineConfig.provider` field + the `toAISDKSystem` / `toAISDKTools` / `toAISDKToolChoice` conversion functions; engine `EngineConfig` is now sole-config (provider-agnostic via `EngineConfig.modelString` only); narrows the public API by 5 type exports + 2 value exports. (f3) `@t2000/cli` + `@t2000/mcp` ESLint flat-configs created (was a CI gap pre-v3.1.0 — `pnpm lint` failed with "No eslint.config.* found" on both packages); `typescript-eslint@^8.58.0` added to devDeps; MCP downgrades `@typescript-eslint/no-unsafe-function-type` to warning to accommodate 14 occurrences in test scaffolding (pragmatic deferred-fix scope, matches the engine package's `no-explicit-any` warning posture). All 4 packages (`sdk`, `engine`, `cli`, `mcp`) bumped 3.0.0 → 3.1.0 in a single commit + tag (`f2bdce39`, `v3.1.0`); npm publish + Vercel auto-deploy of audric @ 3.1.0 all green; 1262 engine tests / 595 SDK / 130 MCP / 35 CLI / 179 web-v2 — all passing. Bundleability + cacheability + concurrency-safety + per-tool flags now resolve by NAME via the central `tool-policy.ts` + `tool-flags.ts` registries — tool objects no longer carry that metadata. Exports renamed: `READ_TOOLS` / `WRITE_TOOLS` (legacy `Tool[]`) → `READ_TOOL_SET` / `WRITE_TOOL_SET` (`ToolSet`) + `READ_TOOL_NAMES` / `WRITE_TOOL_NAMES` (string[]). Co-shipped: M2 audric/web-v2 refactor (chat/route.ts + system-prompt.ts + dispatch-intents.ts + post-write-refresh.ts) — switched to direct `ToolSet` merge, `tool.execute({experimental_context: internalContext})` invocation in the host-side dispatchers, and name-keyed bundle marker assembly. M3 (`@t2000/cli` + `@t2000/mcp`) was a no-op — neither package imports from `@t2000/engine`. Engine smoke: 1262 passed / 10 skipped (real-API gated) / 0 failures; audric/web-v2: 179 passed; CLI 35 / MCP 130 / SDK clean. Marketing site (`apps/web/.../TabbedTerminal.tsx`) touched up: `QueryEngine` → `AISDKEngine`, stale `tools: 37` → `tools: 26`. Test-only `__tests__/_helpers/call-tool-body.ts` adapter (with `defineToolForTest` + `legacyToolView` + `makeGuardView` + `asToolSet` + `callToolBody` + `callToolPreflight` + `zodSchemaOf`) added so the engine's existing test suites (which assume the legacy `tool.call(input, ctx) → {data, displayText}` shape and per-tool `.flags` / `.permissionLevel` properties) continue to typecheck + execute without rewriting them — production code goes through `wrapEngineExecute`, tests go through the adapter (non-enumerable `__t2000_callBody` / `__t2000_preflight` side-doors set inside `wrapEngineExecute`).
- ✅ **P4.2** `spec/reference/CANVAS_VS_ARTIFACT.md` SHIPPED (S.305 — 2026-05-24). Locks canvases as read-only inline primitives (9 fixed templates in `packages/engine/src/tools/canvas.ts`); the chatbot template's artifact pattern is the right shape for Audric Store Phase 5's generated content (ebooks / music / art), not for retrofitting onto canvases. Doc lays out why canvas vs artifact serve different UX (inline read-only vs side-panel editable-with-version-history), the ~5-7 week migration cost for zero user-facing improvement, and the future commitment: `render_artifact` ships as a SEPARATE tool when Audric Store lands.
- 🚫 **P4.3** Subagent pilot for `portfolio_analysis` — **DEFERRED** (S.311 — 2026-05-24, see `spec/reference/SUBAGENT_PILOT_DEFERRAL.md`). Closed as "no current pain." The three plan rationales (context-budget pressure, progress-stream UX, no-needsApproval safety) don't hold up when measured against 2026-05-24 production reality: `portfolio_analysis` returns ~500-800 tokens (not the plan's speculated 5-10k), an 800ms operation doesn't need progress-stream UX, and the read-only property is intrinsic to the pattern (not a justification). The production smoke that drove S.309 / S.310 surfaced tool-availability + state-resolution bugs, NOT subagent-shaped problems. Decision doc captures 4 named re-litigation triggers (context-budget pressure becomes measurable, new context-heavy tool ships, Audric Store Phase 5 generation tasks ship, ≥3 user complaints about a verb feeling stalled) — each with measurement thresholds — plus the full canonical pilot architecture documented inline so a future agent can execute it surgically when a trigger fires. Anti-pattern flag against re-implementing the Vercel doc's example rationale without new evidence.
- ✅ **P4.4** `spec/reference/PRISMA_VS_DRIZZLE.md` SHIPPED (S.306 — 2026-05-24). Locks Audric on Prisma; the chatbot template's Drizzle choice is correct for the template's edge-runtime default but wrong for audric/web-v2's Node-runtime `/api/chat`. Doc captures the 5 evidence pillars: (1) we just migrated FROM Drizzle TO Prisma in S.247, (2) ~1500-2500 LoC of risky edits concentrated in JSON columns, (3) no edge-runtime benefit applies, (4) no painful Prisma surface today (zero `$transaction`, zero `$extends`), (5) the template's schema patterns can inform without ORM migration. Explicit "do not re-litigate without new evidence" lock + 5 revisit triggers.
- ✅ **P4.5** `spec/reference/LONG_RUNNING_WORKFLOWS.md` SHIPPED (S.307 — 2026-05-24). Locks chat on inline `streamText` + `SPEC_AUDRIC_STREAM_RESUME` durability layer; workflows aren't a fit until Audric Store Phase 5 ships generation tasks (10s-2min durations, user expects to leave-and-come-back, server-side work continues without client). Doc explains the workflows fit only when generation tasks land — `render_artifact` will be the first workflow consumer with checkpoint-per-chapter / per-track / per-image semantics. Explicit anti-pattern flag against retrofitting workflows onto chat absent demonstrable stream-resume failure.
- ✅ **P4.6** `spec/reference/LLM_CACHING_DECISION.md` SHIPPED (S.308 — 2026-05-24). Locks Audric on `providerOptions.gateway.caching: 'auto'` (Anthropic prompt cache via AI Gateway) as the only safe LLM caching layer; Redis response cache would be structurally unsafe because every meaningful finance answer references stale-able state (balances, positions, prices, recent activity). Cache invalidation for "any prompt referencing balance / position data" is effectively every prompt, defeating caching. Doc captures the worked failure mode (cached "withdraw $487 safely" served 5min after user already withdrew $300) and clarifies the boundary between LLM response caching (declined) and tool-result caching (already shipped via `lib/portfolio.ts` 60s with P3.4 post-write invalidation — different concern). Explicit anti-pattern flag against re-asking the question.
- ✅ **P4.7** USD-aware auto-execute rule correction SHIPPED (S.304 — 2026-05-24). Closes the F-10 entry from `spec/archive/v07a/SMOKE_PLAN_2026-05-18.md` ("CLAUDE.md / safeguards-defense-in-depth.mdc say 'auto-execute active in audric/web today' but `v2/need-approval.ts:113-115` short-circuits when `!ctx.agent` (always true for audric's client-signed zkLogin flow)"). Per the Round 3 architectural lock: Audric Passport's "you decide" trust pillar means every write taps to confirm by design — auto-execute is NOT a roadmap item. Four edits (not three — the plan listed three but `CLAUDE.md` carried a companion claim that needed the same fix): (1) `.cursor/rules/safeguards-defense-in-depth.mdc` — reframed the B.4 section + the "Audric/web's wiring" subsection + the trust-model subsection to accurately describe "computed and persisted for analytics, but auto-execute structurally disabled under zkLogin"; (2) `CLAUDE.md` "Granular USD-aware permissions" callout — same correction; (3) `.cursor/rules/agent-harness-spec.mdc` — added a contextual note to the `sessionSpendUsd` hook bullet clarifying that the gating itself is inert in web-v2 but the spend ledger increment is still load-bearing for analytics; (4) `audric/apps/web-v2/app/api/chat/route.ts:~L862-876` — replaced the stale "When v0.7d Phase 1+ activates auto-tier writes, wire the increment in `translateChunk` → tool-result" TODO comment with an accurate description: the increment IS wired (P3.4 / S.301) via `onAutoExecuted` hook, auto-execute is intentionally dormant per Round 3. Verify: `rg "active in audric/web today"` returns zero hits in live rules (only hit is the archived F-10 entry itself, expected). The B.4 resolver code STAYS — tested + cheap to keep + honored by `@t2000/cli` consumers with a server-signing agent + future host variants could re-activate.

### Deferred to follow-up SPEC

- `SPEC_BUNDLE_CAP_REMOVAL.md` — P7.6 (cap policy design doc) + P7.7 (BatchPermissionCard implementation). Trigger criteria: ≥10 users hit cap in 4 weeks OR Audric Pay ships send-to-many verb OR business use case lands.

## 4. Engine version cadence

- ✅ After Phase 1: no bump (host-only)
- ✅ After Phase 2: minor → **engine v2.19.3** (P2.1 schema nullable conversion)
- After Phase 5: no engine bump (host-only)
- After Phase 6: no engine bump unless we export a shared `sanitizeStreamError` helper
- After Phase 3: minor (intent classifier hook, repair, skill block wiring)
- After Phase 7.2: SDK minor bump (composeTx accepts `inputCoinFromStep`); engine minor possible
- After Phase 4.1: **major v3.0.0** (Tool type removed from public API)
- After P4.1 follow-up: **minor v3.1.0** (dead `LLMProvider` pathway + `AISDKAnthropicProvider` removed; ESLint configs landed in CLI / MCP; Audric Prisma `todoUpdateCount` column dropped)

## 5. Cross-references

- **Working plan** (full per-item rationale + AI SDK doc citations) → `.cursor/plans/ai_sdk_hardening_bc37c5e8.plan.md`
- **Stream resume sub-SPEC** → `spec/active/shipping/SPEC_AUDRIC_STREAM_RESUME.md` (P2.2)
- **Build tracker entries** (founder-local, gitignored) → `audric-build-tracker.md` S.284 (Phase 1), S.285 (Phase 2 4/5), S.286-S.289 (P2.2 / Stream Resume), S.290-S.292 (Phase 5), S.298 (Phase 6), S.300-S.303 (Phase 3), S.304 (P4.7), S.305-S.308 (P4.2 + P4.4 + P4.5 + P4.6 decision docs), S.309 (P3.1 smoke hotfix — carryover + widened general + dead temperature config removal), S.310 (canvas positionFetcher fallback — render_canvas no longer defaults to $1000/4.5% when serverPositions is unset), S.311 (P4.3 subagent pilot DEFERRED — `spec/reference/SUBAGENT_PILOT_DEFERRAL.md`), **S.314 (Phase 7 P7.2 + P7.3 — `inputCoinFromStep` + `cetusRoute` end-to-end)**
- **Decision docs** (tracked under `spec/reference/`) → `CANVAS_VS_ARTIFACT.md` (P4.2), `PRISMA_VS_DRIZZLE.md` (P4.4), `LONG_RUNNING_WORKFLOWS.md` (P4.5), `LLM_CACHING_DECISION.md` (P4.6), `MCP_PROMPTS_INTEGRATION_DECISION.md` (P3.3)
- **Smoke checklists** (tracked under `spec/active/shipping/` while in flight, `archive/` after pass) → `SPEC_AI_SDK_HARDENING_V3_SMOKE.md` (v3.0.0 + v3.1.0 + S.314 — covers HITL save/borrow/swap + `update_todo` regression + chain-mode bundle + read tools sample); `archive/v07e/SPEC_AI_SDK_HARDENING_PHASE_5_SMOKE.md` (Phase 5, PASSED 2026-05-24)
- **Spec inventory** → `spec/SPEC_INVENTORY_SSOT.md`

## 6. Promotion criteria

This SPEC moves to `spec/archive/v07f/` (or whichever version is active when it closes) when:

- All 7 phases marked SHIPPED above OR
- A clean deferral is documented for any open phase (with trigger criteria for a follow-up SPEC)

Until then it stays in `spec/active/shipping/` so the next session can pick up where the last one left off without re-litigating completed work.
