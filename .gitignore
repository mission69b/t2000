# dependencies
node_modules/

# build output
dist/
.next/
out/
build/

# turbo
.turbo/

# env
.env*

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*
pnpm-debug.log*

# vercel
.vercel

# wallet keys (never commit)
*.key

# infra generated
infra/.env.infra

# spec/ — entire tree is local-only / not part of the public repo
spec/

# marketing — wiped in S.20; ignore by default in case someone recreates locally
marketing/

# raise / investor docs — local reference only
audric-raise-strategy.canvas.tsx
audric-litepaper.html

# private working docs (S.23 2026-04-27 repo privacy sweep) — kept locally,
# untracked from the public mirror. See audric-build-tracker.md S.23 entry
# (itself private now) for rationale.
audric-build-tracker.md
PRODUCT_ROADMAP.md
HANDOFF_NEXT_AGENT.md

# local blog-cover render tool (Playwright source → audric/public/blog/*.png);
# scratch tooling, not part of the public repo root allowlist. Kept locally for
# re-rendering future blog post images.
blog-images.html

# MCP registry tokens
.mcpregistry_*

# pnpm store
.pnpm-store/
.smoke-jwt
.smoke-addr
.smoke-runner.mjs
.smoke-stack.mjs
.smoke-logs/

# Designer deliverables (reference only — deleted once the family design
# rollout ships; assets are copied into each app's public/ as they land)
t2000-design/
T2000_WHITEPAPER.md
