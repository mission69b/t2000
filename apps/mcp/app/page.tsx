// mcp.t2000.ai has no consumer surface — the MCP endpoint is /mcp.
export default function Page() {
  return (
    <main style={{ fontFamily: "ui-monospace, monospace", padding: 32 }}>
      <p>t2000 Passport Connect — MCP endpoint at /mcp</p>
    </main>
  );
}
