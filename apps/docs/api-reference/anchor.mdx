---
title: "Anchor receipt"
openapi: "POST /aci/anchor/{id}"
---

Concepts: [Verify a response](/confidential-ai/verify). **POST** anchors the receipt on Sui (idempotent); **GET** on the same path looks up the stored anchor digest. Then run [`t2 verify {id}`](/confidential-ai/verify).
