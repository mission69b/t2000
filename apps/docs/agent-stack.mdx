---
title: "Architecture"
description: "How t2000 composes — one identity, one wallet, one balance for the agent economy on Sui. Hold and move USDC, pay for intelligence per call, prove who you are."
---

**t2000** is everything an agent needs to act with money in the agent economy on Sui — one identity, one wallet, one balance, for **machines and humans**. Each piece is narrow on purpose and composes with the others.

<CardGroup cols={2}>
  <Card title="Agent Wallet" icon="wallet" href="/agent-wallet">
    The command surface for the whole stack — hold + move USDC gasless, and drive identity and inference from one `t2` CLI, the MCP server, and the skills your agent reads on demand.
  </Card>
  <Card title="Agent Payments" icon="credit-card" href="/pay-any-api">
    Pay any API per call in USDC over [x402](/pay-any-api) — no key, no account. Every major AI + data API on the gateway.
  </Card>
  <Card title="Agent ID" icon="fingerprint" href="/agent-id">
    A portable on-chain identity — handle, owner, profile, and the public [directory](https://t2000.ai). Register gaslessly with one command.
  </Card>
  <Card title="Agent SDK" icon="code" href="/agent-sdk">
    `@t2000/sdk` — the TypeScript surface for programmatic wallets, gasless transfers, Cetus swaps, and x402 pay.
  </Card>
</CardGroup>

---

## How it composes

A typical agent: gets an **identity** ([Agent ID](/agent-id)), funds a **wallet** ([Agent Wallet](/agent-wallet)), and **pays** other APIs as it works ([Agent Payments](/pay-any-api)) — all built with the [SDK](/agent-sdk) or driven from the CLI. Manage your agents, jobs and USDC in the console at [t2000.ai/manage](/console); model keys and credit live on [Audric](/private-inference).

---

## Start here

New to t2000? The **[Quickstart](/quickstart)** installs the CLI, creates a wallet, and gets you to a first payment in five minutes. To call models, see the [Private Inference](/private-inference).
